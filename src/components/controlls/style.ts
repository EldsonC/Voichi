import styled from "styled-components";

export const Controller = styled.footer `
  width: 100vw;
  position: fixed;
  bottom: 60px;

  display: flex;
  justify-content: center;
  align-items: center;

  .content_buttons {
    display: flex;
    align-items: center;
    gap: 26px;
  }
`;